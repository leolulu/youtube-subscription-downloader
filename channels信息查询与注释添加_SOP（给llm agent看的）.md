# YouTube频道信息查询与注释添加 SOP（标准操作程序）

## 一、核心目标

为频道ID列表文件中的每个频道ID添加包含频道名称和内容介绍的注释，格式为：`# 频道名称（内容介绍）`

## 二、详细要求

### 2.1 数据质量要求

**频道名称要求**：
- 必须准确，与YouTube频道官方名称一致
- 优先使用YouTube主页面显示的频道名称
- 如果存在多个名称变体，选择最官方的版本

**内容介绍要求**：
- 必须使用中文表述
- 长度严格控制在不超过一句话
- 精炼体现频道主要内容，避免冗长描述
- 必须具有区分度，能与其他频道清晰区分开
- 介绍内容应基于频道实际内容，避免主观臆测

### 2.2 格式规范

**注释位置要求**：
- 注释行必须位于频道ID行的正上方
- 注释与频道ID之间不应有空行
- 保持文件原有的行结构和缩进格式

**注释格式要求**：
- 统一格式：`# 频道名称（内容介绍）`
- 频道名称和内容介绍之间用中文括号包裹
- 内容介绍本身不包含括号，除非是特定术语

**文件结构要求**：
- 保持原有的注释行（如文件头部的说明）不变
- 只修改频道ID对应的注释行
- 不改变频道ID行的内容和顺序

**频道ID格式要求**：
- 频道ID行必须是纯文本，**绝对不能包含"@"符号**
- 频道ID应该直接书写，不添加任何前缀或后缀
- 示例：正确格式为`马克的技术工作坊`，而不是`@马克的技术工作坊`
- 注意：虽然在YouTube URL中可能看到`@`符号（如`youtube.com/@频道名`），但在文件中录入时必须去掉

### 2.3 输出要求

**完整性要求**：
- 文件中的每个频道ID都必须有对应的注释
- 不能遗漏任何一个频道
- 不能重复添加注释

**一致性要求**：
- 所有频道的注释格式必须完全一致
- 内容介绍的长度和风格应保持统一
- 频道名称的呈现方式应一致

**准确性要求**：
- 频道名称必须准确无误
- 内容介绍必须真实反映频道主题
- 避免错误信息或误导性描述

## 三、质量检查清单

### 信息准确性检查
- [ ] 所有频道名称与YouTube官方名称一致
- [ ] 频道内容介绍准确反映频道主题
- [ ] 没有拼写错误或名称错误
- [ ] 没有错误的内容分类

### 格式规范性检查
- [ ] 所有注释都位于频道ID行的正上方
- [ ] 注释格式统一为"频道名称（内容介绍）"
- [ ] 内容介绍不超过一句话
- [ ] 所有介绍使用中文
- [ ] 括号使用正确（中文括号）
- [ ] 注释符号#使用正确
- [ ] 频道ID不包含"@"符号或其他前缀
- [ ] 频道ID格式为纯文本

### 完整性检查
- [ ] 文件中的每个频道ID都有对应注释
- [ ] 没有遗漏的频道
- [ ] 没有重复的注释
- [ ] 文件结构完整无损坏
- [ ] 原有的非频道注释保持不变

### 可读性检查
- [ ] 内容介绍简洁明了
- [ ] 不同频道之间可以清晰区分
- [ ] 没有生僻难懂的词汇
- [ ] 介绍长度适中，便于阅读

## 四、常见问题处理

### 问题1：查询到多个相似频道名称
**处理原则**：访问频道页面确认官方名称，优先使用YouTube主页面显示的名称

**判断依据**：
- 频道页面顶部的名称
- 频道简介中的自称
- 搜索结果中优先显示的名称

### 问题2：频道信息不完整
**处理原则**：基于可获取信息进行推断，必要时补充查询

**处理方式**：
- 根据频道视频标题和主题推断内容类型
- 参考第三方统计网站的频道描述
- 如果仍无法确定，在介绍中标注"推测"字样
- 避免完全虚构内容介绍

### 问题3：内容介绍难以精炼到一句话
**处理原则**：提取核心关键词，使用简洁表达

**处理方式**：
- 提取最核心的2-4个关键词
- 使用"/"分隔不同内容类型
- 示例："科技/数码产品评测"
- 示例："个人成长与自我提升"
- 避免使用复杂句式

### 问题4：频道内容类型模糊或混合
**处理原则**：选择最主要的内容类型进行描述

**处理方式**：
- 识别频道最核心的内容领域
- 如果有多个主要领域，用"/"分隔
- 优先描述最频繁出现的内容类型
- 示例："时事评论与泛知识"

### 问题5：频道ID格式错误
**错误表现**：频道ID前面错误地添加了"@"符号或其他前缀

**正确格式**：
- ✅ 马克的技术工作坊
- ✅ kedaibiao
- ✅ MoneyXYZ

**错误格式**：
- ❌ @马克的技术工作坊
- ❌ @kedaibiao
- ❌ https://youtube.com/@马克的技术工作坊

**处理方式**：
- 频道ID必须是纯文本，不能包含"@"符号
- 不能包含YouTube URL或完整链接
- 只保留频道handle本身

### 问题6：文件更新过程中出现格式错误
**处理原则**：立即停止，恢复正确状态

**处理方式**：
- 立即停止更新操作
- 回滚到上一个正确版本
- 重新检查并修正问题行
- 必要时备份原始文件

## 五、最佳实践建议

1. **进度追踪**：建立处理清单，记录已处理和待处理的频道，避免遗漏

2. **先行验证**：建议先处理2-3个频道，确认流程和格式正确后再全面展开

3. **备份保护**：修改前保留原始文件副本，便于出错时恢复

4. **逐步验证**：每处理几个频道就检查一次格式和内容，避免批量错误

5. **信息交叉验证**：对于重要频道，可以从多个来源（频道页面、视频标题、第三方统计）交叉验证信息准确性

6. **保持客观**：内容介绍应基于频道实际内容，避免个人偏好或主观评价

7. **定期检查**：如果处理大量频道，建议定期检查已完成的部分，确保格式一致性

## 六、快速参考

**目标格式示例**：
```
# 一口新飯（财务教育与投资理财）
MoneyXYZ

# Junchi Cheung（情感与两性关系谈话）
junchicheung

# stone记（时政评论与科技谈话）
stone_ji
```

**有效内容介绍示例**：
- ✅ 一口新飯（财务教育与投资理财）
- ✅ Junchi Cheung（情感与两性关系谈话）
- ✅ stone记（时政评论与科技谈话）
- ✅ EllaTalks（英语学习与口语教学）
- ✅ 北美运动学博士Bruce_PhD（健身科普与运动科学）
- ✅ Shuyi Wang（AI科技与产品设计）

**无效内容介绍示例**：
- ❌ 一口新飯 - 缺少内容介绍
- ❌ 一口新飯（这是一个讲财务教育的频道，内容包括投资理财、FIRE运动等）- 太长，超过一句话
- ❌ 一口新飯(财务教育与投资理财) - 使用了英文括号
- ❌ MoneyXYZ（财务教育与投资理财）- 频道名称错误，应该是"一口新飯"

**频道ID格式错误示例**：
- ❌ @马克的技术工作坊 - 频道ID包含了"@"符号
- ❌ @kedaibiao - 频道ID包含了"@"符号
- ❌ https://www.youtube.com/@马克的技术工作坊 - 包含了完整URL
- ✅ 马克的技术工作坊 - 正确格式
- ✅ kedaibiao - 正确格式
